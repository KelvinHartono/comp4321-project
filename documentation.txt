db design


-integers should be converted into string type when add to db

-all related keys and values string type are processed into byte for storing in DB

-all keys and values inside all DB are byte type
